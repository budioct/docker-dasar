# --DOCKER IMAGE--
# melihat list image yang sudah di install
docker image ls
docker images

# dwonload image yang sudah ada di docker hub
docker image pull redis:lates
docker image pull redis:tag-version

# menghapus image
docker image rm redis:lates